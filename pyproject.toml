[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "project-name"
version = "0.1.0"
description = "One-line description (copy from portfolio doc)."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["app*"]
